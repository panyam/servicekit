{
  "name": "@panyam/servicekit-client",
  "version": "0.0.7",
  "description": "TypeScript client for ServiceKit WebSocket and SSE protocols",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "browser": "dist/servicekit-client.browser.js",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc && npm run build:esm && npm run build:browser",
    "build:esm": "esbuild src/index.ts --bundle --format=esm --outfile=dist/index.mjs",
    "build:browser": "esbuild src/index.ts --bundle --format=iife --global-name=ServiceKit --outfile=dist/servicekit-client.browser.js",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "grpc",
    "websocket",
    "streaming",
    "sse",
    "server-sent-events",
    "protobuf",
    "typescript",
    "servicekit"
  ],
  "author": "panyam",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/panyam/servicekit.git",
    "directory": "clients/typescript"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
